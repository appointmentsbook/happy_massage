<% if massage.scheduled? || massage.missed? %>
  <%= link_to('', confirm_presence_admin_schedule_path(massage, authenticity_token: form_authenticity_token), method: 'put', remote: true, class: 'ls-btn ls-ico-thumbs-up') %>
<% end %>
<% if massage.scheduled? || massage.attended? %>
  <%= link_to('', confirm_absence_admin_schedule_path(massage, authenticity_token: form_authenticity_token), method: 'put', remote: true, class: 'ls-btn-primary ls-ico-thumbs-up2') %>
<% end %>
